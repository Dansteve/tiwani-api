"""Owner-run operational scripts for tiwani-api (not part of the served app).

Currently: seed_demo_data.py (seed a realistic demo account into a DEMO / throwaway
Supabase project) and its pure blueprint demo_data.py. These are run by the owner from
the repo root; nothing here is imported by the FastAPI app at runtime.
"""
